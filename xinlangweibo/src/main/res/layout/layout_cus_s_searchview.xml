<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:background="@color/bg_gray"
    android:layout_height="wrap_content">

    <RelativeLayout
        android:id="@+id/card_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="2dp"
        android:background="@drawable/corners_bg"
        android:padding="4dp">

        <TextView
            android:id="@+id/tv_search_hint"
            style="@style/middle_light_gray_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_gravity="center_vertical"
            android:text="@string/searchView_hint" />

        <ImageView
            android:id="@+id/iv_search"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_toLeftOf="@id/tv_search_hint"
            android:background="@drawable/userinfo_tabicon_search_highlighted"
            android:backgroundTint="@color/font_normal"
            android:scaleType="fitCenter" />

    </RelativeLayout>
</RelativeLayout>
